Script to check the logs on all the pods in a spacific namespace:

# pods_log_script.sh

for pod in $(kubectl get pods -n longhorn-system -o name); do
  echo "Logs for $pod:"
  kubectl logs -n longhorn-system $pod | grep vol-test02
  echo "----------------------------------------"
done

Note:
If the Kubernetes cluster was created using Ranger, run the following command before executing the above script:
$ alias kubectl='/var/lib/rancher/rke2/bin/kubectl --kubeconfig /etc/rancher/rke2/rke2.yaml'

